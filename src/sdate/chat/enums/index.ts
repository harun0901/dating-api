export enum MessageStatusType {
  Read = 'READ',
  Mailed = 'MAILED',
}

export enum ChatDefault {
  SENDER_DELETE = 0,
  RECEIVER_DELETE = 0,
  SEEN = 0,
}

export enum ChatValues {
  DELETED = 1,
  SEEN = 1,
}
